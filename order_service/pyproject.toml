[project]
name = "order-service"
version = "0.0.1"
description = "Application for serving users order"
authors = [
    {name = "Никита",email = "nervok1337@gmail.com"}
]
license = "MIT"
readme = "README.md"
requires-python = "^3.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
